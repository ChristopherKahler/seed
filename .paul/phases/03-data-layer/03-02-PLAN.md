---
phase: 03-data-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [templates/planning-application.md, templates/planning-workflow.md, templates/planning-client.md, templates/planning-utility.md, templates/planning-campaign.md]
autonomous: true
---

<objective>
## Goal
Migrate and adapt the 5 PLANNING-{type}.md templates from the workspace root (`templates/`) into the SEED skill directory (`templates/planning-{type}.md`).

## Purpose
The ideate task's `generate_planning` step reads `templates/planning-{type}.md` to produce PLANNING.md output. Without these templates, ideate can't generate structured output. The templates define what a complete PLANNING.md looks like for each project type.

## Output
- 5 template files in `templates/` directory
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Source Files (migrate from these)
@~/chris-ai-systems/templates/PLANNING-application.md (202 lines)
@~/chris-ai-systems/templates/PLANNING-workflow.md (141 lines)
@~/chris-ai-systems/templates/PLANNING-client.md (167 lines)
@~/chris-ai-systems/templates/PLANNING-utility.md (116 lines)
@~/chris-ai-systems/templates/PLANNING-campaign.md (150 lines)

## How Templates Are Used
- tasks/ideate.md step `generate_planning` reads `templates/planning-{type}.md`
- Template is populated with conversation content
- Output written to `projects/{name}/PLANNING.md`
- PLANNING.md must be rich enough for headless PAUL init
</context>

<skills>
No SPECIAL-FLOWS.md — skills section omitted.
</skills>

<acceptance_criteria>

## AC-1: All 5 Templates Exist
```gherkin
Given the template migration is complete
When checking templates/ directory
Then 5 files exist: planning-application.md, planning-workflow.md, planning-client.md, planning-utility.md, planning-campaign.md
```

## AC-2: Templates Adapted Not Just Copied
```gherkin
Given a migrated template
When compared to the source
Then file naming follows skill convention (planning-{type}.md not PLANNING-{type}.md)
And placeholder syntax uses {curly braces} for variables
And templates are self-contained (no references to workspace-root paths)
```

## AC-3: Templates Align with Data Layer
```gherkin
Given a template for any type
When compared to that type's data/guide.md
Then template sections match guide sections (same topics covered)
And template section order follows guide section order
```

## AC-4: Metadata Block Present
```gherkin
Given any template
When read by the generate_planning step
Then it includes a metadata block at the top (Type, Skill Loadout, Quality Gates)
And the metadata aligns with data/{type}/skill-loadout.md recommendations
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Migrate and adapt 5 PLANNING templates</name>
  <files>templates/planning-application.md, templates/planning-workflow.md, templates/planning-client.md, templates/planning-utility.md, templates/planning-campaign.md</files>
  <action>
    For each of the 5 types, read the source template from the workspace root and write an adapted version to the skill's templates/ directory.

    **Adaptations to make:**
    1. Rename: `PLANNING-{type}.md` → `planning-{type}.md` (lowercase, skill convention)
    2. Ensure placeholder syntax uses `{curly braces}` consistently
    3. Remove any workspace-root-specific references (paths should be relative to project)
    4. Verify section structure aligns with the corresponding `data/{type}/guide.md` sections
    5. Ensure metadata block at top includes Type, Skill Loadout, Quality Gates
    6. Keep the templates comprehensive — they define the output quality of ideation

    **Process per template:**
    1. Read source: `~/chris-ai-systems/templates/PLANNING-{TYPE}.md`
    2. Read corresponding: `data/{type}/guide.md` (for section alignment check)
    3. Adapt and write to: `templates/planning-{type}.md`

    **What to avoid:**
    - Do NOT strip content from templates — they should remain comprehensive
    - Do NOT add new sections that aren't in the source or guide — adapt, don't expand
    - Do NOT modify the source templates at workspace root
  </action>
  <verify>
    1. Count files: `ls templates/ | wc -l` should be 5
    2. All filenames lowercase: planning-application.md, planning-workflow.md, etc.
    3. Each template has metadata block (grep for "Type:" and "Skill Loadout:")
    4. No references to workspace root paths (grep for "chris-ai-systems" should find nothing)
    5. Placeholder syntax: grep for {curly} patterns present
  </verify>
  <done>AC-1 through AC-4 satisfied: 5 adapted templates with correct naming, metadata, and section alignment</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- seed.md, tasks/*.md, data/**/* (all prior phases, locked)
- Source templates at ~/chris-ai-systems/templates/ (read only, do not modify)

## SCOPE LIMITS
- This plan creates ONLY templates/ files
- Do NOT create checklists/ files (Phase 4)
- Adapt existing templates, do not create new content

</boundaries>

<verification>
Before declaring plan complete:
- [ ] 5 template files exist in templates/
- [ ] All filenames follow planning-{type}.md convention
- [ ] Each has metadata block (Type, Skill Loadout, Quality Gates)
- [ ] No workspace-root path references
- [ ] Section structure aligns with corresponding guide.md
- [ ] Templates are comprehensive (not stripped down)
</verification>

<success_criteria>
- All 5 templates migrated and adapted
- Templates align with data layer (guide sections match)
- Ideate task can load templates for output generation
</success_criteria>

<output>
After completion, create `.paul/phases/03-data-layer/03-02-SUMMARY.md`
</output>
